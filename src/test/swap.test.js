// import { render } from "./test-utils";

// import Sell from "../pages/sell";

describe("Swap page", () => {
  test("Swap page matches snapshot", () => {
    // const { asFragment } = render(<PrivateIndividual />);
    // expect(asFragment()).toMatchSnapshot();
    expect(true).toBe(true);
  });
});
